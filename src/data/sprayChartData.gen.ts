import type { BattedBallEvent } from '../types/battedBall'

/** Populated by `npm run fetch-data` — Statcast samples keyed by player display name. */
export const SPRAY_EVENTS_BY_PLAYER_NAME: Record<string, BattedBallEvent[]> = {}
