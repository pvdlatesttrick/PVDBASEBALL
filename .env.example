# Copy to `.env` and fill in. Vite only exposes variables prefixed with `VITE_`
# to client-side code — access them via `import.meta.env.VITE_*` (see src/config/env.ts).

VITE_ODDS_API_KEY=

# Build-time only (used in vite.config.ts, not bundled into the app):
# GITHUB_PAGES=true
